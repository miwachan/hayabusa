#define DEBUG#ifdef DEBUG#define DebugPrint( message ) 		printf( message )#else#define DebugPrint( message ) #endif#define ATOM 	"Si"			/* 原子種　*/// MAX_ATOMS = 50000;	/* 最大原子数 このヘッダファイルの中で定義したいんだけど　*/static double ATOMIC_DISTANCE_Si_Si = 2.351258;	/* Siの原子間距離(A) */static double A_Lattice = 5.431; // 格子定数static double PI = 3.14159265358979323846;struct atoms {		int number;	 	char element[255];		double x;		double y;		double z;		int neighbor[4];		int type;		} ATOMS[500000];/*typedef struct vector {		double x;		double y;		double z;		} VECTOR;*/